/**
 * Q04 — House Robber
 * Difficulty: Easy
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/04-house-robber.md
 */

function rob(nums) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(rob([1, 2, 3, 1]));     // 4
console.log(rob([2, 7, 9, 3, 1]));  // 12
console.log(rob([2, 1, 1, 2]));     // 4

module.exports = { rob };
