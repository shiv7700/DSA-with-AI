/**
 * Q14 — Detect a Cycle
 * Difficulty: Medium
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/14-detect-cycle.md
 */

function hasCycle(head) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// 1->2->3->4->2 (cycle at node 2) → true
// 1->2->3->4->null                → false

module.exports = { hasCycle };
