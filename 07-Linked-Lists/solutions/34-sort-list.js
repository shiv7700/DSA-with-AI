/**
 * Q34 — Sort a Linked List
 * Difficulty: Hard
 * Expected:   O(n log n) time · O(log n) space
 * Problem:    ../questions/34-sort-list.md
 */

function sortList(head) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// List: 4->2->1->3 → 1->2->3->4->null

module.exports = { sortList };
