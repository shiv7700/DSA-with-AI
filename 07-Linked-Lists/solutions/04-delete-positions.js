/**
 * Q4 — Delete at Head, Tail, and Index
 * Difficulty: Easy
 * Expected:   O(1) head · O(n) tail/index · O(1) space
 * Problem:    ../questions/04-delete-positions.md
 */

function deleteAtHead(head) {
  // TODO: your solution here
}

function deleteAtTail(head) {
  // TODO: your solution here
}

function deleteAtIndex(head, index) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// deleteAtHead on 1->2->3 → head.val === 2
// deleteAtTail on 1->2->3 → tail.val === 2
// deleteAtIndex on 1->2->3->4, index=2 → removes 3

module.exports = { deleteAtHead, deleteAtTail, deleteAtIndex };
