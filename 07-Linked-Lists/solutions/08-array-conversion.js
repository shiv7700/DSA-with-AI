/**
 * Q8 — Array to Linked List and Back
 * Difficulty: Easy
 * Expected:   O(n) time · O(n) space
 * Problem:    ../questions/08-array-conversion.md
 */

function arrayToList(arr) {
  // TODO: your solution here
}

function listToArray(head) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(listToArray(arrayToList([1, 2, 3, 4]))); // [1, 2, 3, 4]

module.exports = { arrayToList, listToArray };
