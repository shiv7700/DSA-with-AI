/**
 * Q1 — Implement ListNode Class
 * Difficulty: Easy
 * Expected:   N/A — foundational building block
 * Problem:    ../questions/01-implement-listnode.md
 */

class ListNode {
  constructor(val = 0, next = null) {
    // TODO: your solution here
  }
}

// ── quick tests ──────────────────────────────────────────────
const a = new ListNode(42);
console.log(a); // ListNode { val: 42, next: null }

module.exports = { ListNode };
