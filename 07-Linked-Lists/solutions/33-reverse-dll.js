/**
 * Q33 — Reverse a Doubly Linked List
 * Difficulty: Hard
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/33-reverse-dll.md
 */

function reverseDLL(head) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// null<-1<->2<->3<->4->null → null<-4<->3<->2<->1->null

module.exports = { reverseDLL };
