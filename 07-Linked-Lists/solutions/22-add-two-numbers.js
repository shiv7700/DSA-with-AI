/**
 * Q22 — Add Two Numbers
 * Difficulty: Medium
 * Expected:   O(max(n, m)) time · O(max(n, m)) space
 * Problem:    ../questions/22-add-two-numbers.md
 */

function addTwoNumbers(l1, l2) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// l1=2->4->3 (342), l2=5->6->4 (465) → 7->0->8->null (807)

module.exports = { addTwoNumbers };
