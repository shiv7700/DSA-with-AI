/**
 * Q3 — Insert at Head, Tail, and Index
 * Difficulty: Easy
 * Expected:   O(1) head · O(n) tail/index · O(1) space
 * Problem:    ../questions/03-insert-positions.md
 */

function insertAtHead(head, val) {
  // TODO: your solution here
}

function insertAtTail(head, val) {
  // TODO: your solution here
}

function insertAtIndex(head, index, val) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// build: 2 -> 3 -> null, insert 1 at head → 1 -> 2 -> 3 -> null
// console.log(insertAtHead(buildList([2,3]), 1)); // head.val === 1

module.exports = { insertAtHead, insertAtTail, insertAtIndex };
