/**
 * Q29 — Merge k Sorted Lists
 * Difficulty: Hard
 * Expected:   O(N log k) time · O(k) space
 * Problem:    ../questions/29-merge-k-sorted-lists.md
 */

function mergeKLists(lists) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// lists=[1->4->5, 1->3->4, 2->6] → 1->1->2->3->4->4->5->6->null

module.exports = { mergeKLists };
