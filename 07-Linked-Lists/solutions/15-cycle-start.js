/**
 * Q15 — Find the Start of a Cycle
 * Difficulty: Medium
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/15-cycle-start.md
 */

function detectCycleStart(head) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// 3->1->0->4->1 (4's next = node(1)) → node with val=1
// 1->2->3->null → null

module.exports = { detectCycleStart };
