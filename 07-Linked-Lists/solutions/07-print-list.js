/**
 * Q7 — Print the List
 * Difficulty: Easy
 * Expected:   O(n) time · O(1) iterative / O(n) recursive space
 * Problem:    ../questions/07-print-list.md
 */

function printIterative(head) {
  // TODO: your solution here
}

function printRecursive(head) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// List: 1->2->3 → "1 -> 2 -> 3 -> null"
// List: null    → "null"

module.exports = { printIterative, printRecursive };
