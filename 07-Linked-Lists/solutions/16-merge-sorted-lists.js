/**
 * Q16 — Merge Two Sorted Lists
 * Difficulty: Medium
 * Expected:   O(n + m) time · O(1) iterative space
 * Problem:    ../questions/16-merge-sorted-lists.md
 */

function mergeTwoLists(list1, list2) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// list1=1->2->4, list2=1->3->4 → 1->1->2->3->4->4->null

module.exports = { mergeTwoLists };
