/**
 * Q27 — Partition List
 * Difficulty: Medium
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/27-partition-list.md
 */

function partition(head, x) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// List: 1->4->3->2->5->2, x=3 → 1->2->2->4->3->5->null

module.exports = { partition };
