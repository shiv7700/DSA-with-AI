/**
 * Q13 — Pow(x, n) — Fast Exponentiation
 * Difficulty: Medium
 * Expected:   O(log n) time · O(log n) space
 * Problem:    ../questions/13-pow-x-n.md
 */

function myPow(x, n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(myPow(2.0, 10)); // 1024.0

module.exports = { myPow };
