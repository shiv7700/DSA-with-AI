/**
 * Q25 — Binary Subarrays With Sum
 * Difficulty: Medium (LeetCode 930)
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/25-binary-subarrays-with-sum.md
 */

function numSubarraysWithSum(nums, goal) {
  // TODO: your solution here (atMost(goal) - atMost(goal-1) trick)
}

// ── quick tests ──────────────────────────────────────────────
console.log(numSubarraysWithSum([1, 0, 1, 0, 1], 2)); // 4

module.exports = { numSubarraysWithSum };
