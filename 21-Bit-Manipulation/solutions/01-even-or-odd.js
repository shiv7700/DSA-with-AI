/**
 * Q1 — Even or Odd via Bits
 * Difficulty: Easy
 * Expected:   O(1) time · O(1) space
 * Problem:    ../questions/01-even-or-odd.md
 */

function isEven(n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(isEven(4)); // true

module.exports = { isEven };
