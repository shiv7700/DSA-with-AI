/**
 * Q3 — Count Set Bits (Popcount)
 * Difficulty: Easy
 * Expected:   O(k) time · O(1) space
 * Problem:    ../questions/03-count-set-bits.md
 */

function countSetBits(n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(countSetBits(11)); // 3

module.exports = { countSetBits };
