/**
 * Q18 — Counting Bits (0 to n)
 * Difficulty: Medium
 * Expected:   O(n) time · O(n) space
 * Problem:    ../questions/18-counting-bits.md
 */

function countingBits(n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(countingBits(2)); // [0, 1, 1]

module.exports = { countingBits };
