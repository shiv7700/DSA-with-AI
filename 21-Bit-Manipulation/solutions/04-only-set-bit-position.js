/**
 * Q4 — Position of the Only Set Bit
 * Difficulty: Easy
 * Expected:   O(log n) time · O(1) space
 * Problem:    ../questions/04-only-set-bit-position.md
 */

function onlySetBitPosition(n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(onlySetBitPosition(1)); // 0

module.exports = { onlySetBitPosition };
