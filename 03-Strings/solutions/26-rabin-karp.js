/**
 * Q26 — Rabin-Karp Rolling Hash
 * Difficulty: Hard
 * Expected:   O(n + m) average · O(m) space
 * Problem:    ../questions/26-rabin-karp.md
 */

function rabinKarp(text, pattern) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(rabinKarp('AABAACAADAABAABA', 'AABA')); // 0

module.exports = { rabinKarp };
