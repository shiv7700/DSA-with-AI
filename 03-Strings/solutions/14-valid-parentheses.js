/**
 * Q14 — Valid Parentheses
 * Difficulty: Medium
 * Expected:   O(n) time · O(n) space
 * Problem:    ../questions/14-valid-parentheses.md
 */

function isValid(str) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(isValid('()')); // true
console.log(isValid('()[]{}')); // true

module.exports = { isValid };
