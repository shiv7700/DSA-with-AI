/**
 * Q5 — Power of a Number in O(log n)
 * Difficulty: Easy
 * Expected:   O(log n) time · O(log n) space
 * Problem:    ../questions/05-power.md
 */

function power(base, exp) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(power(2, 10)); // 1024

module.exports = { power };
