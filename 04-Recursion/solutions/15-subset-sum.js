/**
 * Q15 — Subset Sum: Does Any Subset Add to K?
 * Difficulty: Medium
 * Expected:   O(2^n) time · O(n) space
 * Problem:    ../questions/15-subset-sum.md
 */

function hasSubsetSum(nums, k) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(hasSubsetSum([3, 1, 4, 2, 5], 9)); // true

module.exports = { hasSubsetSum };
