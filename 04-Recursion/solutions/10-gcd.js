/**
 * Q10 — GCD Using Euclid's Algorithm
 * Difficulty: Easy
 * Expected:   O(log(min(a, b))) time · O(log(min(a, b))) space
 * Problem:    ../questions/10-gcd.md
 */

function gcd(a, b) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(gcd(48, 18)); // 6

module.exports = { gcd };
