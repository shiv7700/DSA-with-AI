/**
 * Q2 — Factorial of N
 * Difficulty: Easy
 * Expected:   O(n) time · O(n) space
 * Problem:    ../questions/02-factorial.md
 */

function factorial(n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(factorial(5)); // 120

module.exports = { factorial };
