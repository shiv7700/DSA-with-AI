/**
 * Q23 — Word Break
 * Difficulty: Hard
 * Expected:   O(n² × m) time · O(n) space
 * Problem:    ../questions/23-word-break.md
 */

function wordBreak(s, wordDict) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(wordBreak('leetcode', ['leet', 'code'])); // true

module.exports = { wordBreak };
