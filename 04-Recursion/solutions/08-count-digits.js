/**
 * Q8 — Count Digits of a Number
 * Difficulty: Easy
 * Expected:   O(d) time · O(d) space
 * Problem:    ../questions/08-count-digits.md
 */

function countDigits(n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(countDigits(1234)); // 4

module.exports = { countDigits };
