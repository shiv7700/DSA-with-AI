/**
 * Q23 — Multiply Strings
 * Difficulty: Medium
 * Expected:   O(m × n) time · O(m + n) space
 * Problem:    ../questions/23-multiply-strings.md
 */

function multiplyStrings(num1, num2) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(multiplyStrings('2', '3')); // '6'

module.exports = { multiplyStrings };
