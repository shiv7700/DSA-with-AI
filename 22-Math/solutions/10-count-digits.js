/**
 * Q10 — Count Digits
 * Difficulty: Easy
 * Expected:   O(log n) time · O(1) space
 * Problem:    ../questions/10-count-digits.md
 */

function countDigits(n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(countDigits(5)); // 1

module.exports = { countDigits };
