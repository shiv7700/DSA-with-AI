/**
 * Q5 — Is Array Sorted?
 * Difficulty: Easy
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/05-is-sorted.md
 */

function isSorted(arr) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(isSorted([1, 2, 3, 4, 5])); // 'asc'

module.exports = { isSorted };
