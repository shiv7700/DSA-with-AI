/**
 * Q2 — Min & Max in One Pass
 * Difficulty: Easy
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/02-min-max.md
 */

function minMax(arr) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(minMax([3, 1, 7, 0, 5, 9, 2])); // { min: 0, max: 9 }

module.exports = { minMax };
