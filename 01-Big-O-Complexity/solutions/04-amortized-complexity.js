/**
 * Q4 — Amortized Complexity
 * Difficulty: Medium (Conceptual)
 * Expected:   Written explanation — no executable code required
 * Problem:    ../questions/04-amortized-complexity.md
 */

// Conceptual question — write your answer as a comment below.
//
// Explain amortized complexity using Array.push() as the example.
// Walk through the doubling mechanism and show why the average
// cost per push works out to O(1) even though occasional pushes are O(n).
//
// ── your answer ──────────────────────────────────────────────
//
// Amortized complexity means...
//
// Array.push() is O(1) amortized because:
//   - Most pushes: ...
//   - Occasional resize pushes: ...
//   - Why doubling makes the average O(1): ...
//
// (write here)

// ── optional: simulate the doubling behavior ──────────────────
// function makeDynamicArray() {
//   // TODO: simulate a dynamic array that doubles when full
//   // log each resize event to see how often they occur
// }

module.exports = {};
