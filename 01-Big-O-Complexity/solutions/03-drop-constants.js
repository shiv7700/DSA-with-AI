/**
 * Q3 — Why Drop Constants and Lower-Order Terms?
 * Difficulty: Easy (Conceptual)
 * Expected:   Written explanation — no executable code required
 * Problem:    ../questions/03-drop-constants.md
 */

// Conceptual question — write your answer as a comment below.
//
// Explain why Big-O drops constants (O(3n) → O(n)) and lower-order terms
// (O(n² + n) → O(n²)). Also simplify the following expressions:
//
//   O(n + 500)        → O(?)
//   O(3n² + 2n + 7)   → O(?)
//   O(n log n + n²)   → O(?)
//   O(4)              → O(?)
//
// ── your answer ──────────────────────────────────────────────
//
// Constants are dropped because...
// Lower-order terms are dropped because...
//
// Simplifications:
//   O(n + 500)       → O(?)   explanation: ...
//   O(3n² + 2n + 7)  → O(?)   explanation: ...
//   O(n log n + n²)  → O(?)   explanation: ...
//   O(4)             → O(?)   explanation: ...
//
// (write here)

module.exports = {};
