/**
 * Q23 — Sliding Window Median
 * Difficulty: Hard
 * Expected:   O(n log k) time · O(k) space
 * Problem:    ../questions/23-sliding-window-median.md
 */

function medianSlidingWindow(nums, k) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(medianSlidingWindow([1, 3, -1, -3, 5, 3, 6, 7], 3)); // [1.0, -1.0, -1.0, 3.0, 5.0, 6.0]

module.exports = { medianSlidingWindow };
