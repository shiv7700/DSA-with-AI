/**
 * Q7 — Reverse a Queue
 * Difficulty: Easy
 * Expected:   O(n) time · O(n) space
 * Problem:    ../questions/07-reverse-queue.md
 */

function reverseQueue(queue) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(reverseQueue([1, 2, 3, 4, 5])); // [5, 4, 3, 2, 1]

module.exports = { reverseQueue };
