/**
 * Q21 — Task Scheduler
 * Difficulty: Medium
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/21-task-scheduler.md
 */

function leastInterval(tasks, n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(leastInterval(["A","A","A","B","B","B"], 2)); // 8

module.exports = { leastInterval };
