/**
 * Q18 — Robot Room Cleaner
 * Difficulty: Hard
 * Expected:   O(n) time · O(n) space (n = reachable cells)
 * Problem:    ../questions/18-robot-room-cleaner.md
 */

/**
 * The robot API interface is defined as follows:
 * interface Robot {
 *   move(): boolean;
 *   turnLeft(): void;
 *   turnRight(): void;
 *   clean(): void;
 * }
 */

function cleanRoom(robot) {
  // TODO: your solution here
}

// ── note ──────────────────────────────────────────────────────
// This problem requires a robot API — unit testing requires a mock.
// Build a mock Robot class that wraps a grid and test with it.

module.exports = { cleanRoom };
