/**
 * Q28 — Partition to K Equal Sum Subsets
 * Difficulty: Hard
 * Expected:   O(k · 2^n) time · O(n) space
 * Problem:    ../questions/28-partition-k-equal-sum.md
 */

function canPartitionKSubsets(nums, k) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(canPartitionKSubsets([4,3,2,3,5,2,1], 4)); // true
console.log(canPartitionKSubsets([1,2,3,4], 3));         // false

module.exports = { canPartitionKSubsets };
