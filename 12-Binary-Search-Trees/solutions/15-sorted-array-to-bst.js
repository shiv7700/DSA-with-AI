/**
 * Q15 — Convert Sorted Array to Height-Balanced BST
 * Difficulty: Medium
 * Expected:   O(n) time · O(log n) space
 * Problem:    ../questions/15-sorted-array-to-bst.md
 */

function sortedArrayToBST(nums) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// nums = [-10,-3,0,5,9] → height-balanced BST (root 0)
console.log(sortedArrayToBST([-10, -3, 0, 5, 9])); // TreeNode root

module.exports = { sortedArrayToBST };
