/**
 * Q16 — Convert Sorted Linked List to BST
 * Difficulty: Medium
 * Expected:   O(n log n) time · O(log n) space
 * Problem:    ../questions/16-sorted-list-to-bst.md
 */

function sortedListToBST(head) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// -10 → -3 → 0 → 5 → 9 → height-balanced BST (root 0)
console.log(sortedListToBST(null)); // null — empty list

module.exports = { sortedListToBST };
