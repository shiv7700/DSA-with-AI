/**
 * Q27 — Unique BSTs Count (Catalan Numbers)
 * Difficulty: Medium
 * Expected:   O(n²) time · O(n) space
 * Problem:    ../questions/27-unique-bsts-count.md
 */

function numTrees(n) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// n = 3 → 5  (Catalan number C(3))
// n = 1 → 1
console.log(numTrees(3)); // 5

module.exports = { numTrees };
