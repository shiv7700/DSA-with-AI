/**
 * Q5 — Count Nodes and Leaves
 * Difficulty: Easy
 * Expected:   O(n) time · O(h) space
 * Problem:    ../questions/05-count-nodes-leaves.md
 */

function countNodes(root) {
  // TODO: your solution here
}

function countLeaves(root) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// Tree: 10 with children 5,15 and grandchildren 3,7,20
// countNodes(root) → 6 · countLeaves(root) → 3
console.log(countNodes(null));  // 0
console.log(countLeaves(null)); // 0

module.exports = { countNodes, countLeaves };
