/**
 * Q1 — Insert a Value into a BST
 * Difficulty: Easy
 * Expected:   O(log n) time average · O(log n) space
 * Problem:    ../questions/01-insert-into-bst.md
 */

function insertIntoBST(root, val) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// Input: root = [4,2,7,1,3], val = 5 → tree with 5 inserted
console.log(insertIntoBST(null, 5)); // TreeNode(5) — empty tree case

module.exports = { insertIntoBST };
