/**
 * Q22 — K Closest Values in BST
 * Difficulty: Medium
 * Expected:   O(n) time · O(n) space
 * Problem:    ../questions/22-k-closest-bst.md
 */

function kClosestValues(root, target, k) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// Tree: [4,2,5,1,3], target = 3.714286, k = 2 → [4,3]
console.log(kClosestValues(null, 3.7, 2)); // []

module.exports = { kClosestValues };
