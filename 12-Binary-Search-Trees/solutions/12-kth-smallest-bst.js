/**
 * Q12 — Kth Smallest Element in a BST
 * Difficulty: Medium
 * Expected:   O(h + k) time · O(h) space
 * Problem:    ../questions/12-kth-smallest-bst.md
 */

function kthSmallest(root, k) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// Tree: [3,1,4,null,2], k=1 → 1
// Tree: [5,3,6,2,4,null,null,1], k=3 → 3
console.log(kthSmallest(null, 1)); // undefined/null

module.exports = { kthSmallest };
