/**
 * Q7 — Candy
 * Difficulty: Hard
 * Expected:   O(n) time · O(n) space
 * Problem:    ../questions/07-candy.md
 */

function candy(ratings) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(candy([1, 0, 2])); // 5

module.exports = { candy };
