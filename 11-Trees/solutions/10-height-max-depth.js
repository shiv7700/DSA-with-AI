/**
 * Q10 — Height / Max Depth
 * Difficulty: Easy
 * Expected:   O(n) time · O(h) space
 * Problem:    ../questions/10-height-max-depth.md
 */

function maxDepth(root) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// Tree: [3, 9, 20, null, null, 15, 7] → 3

module.exports = { maxDepth };
