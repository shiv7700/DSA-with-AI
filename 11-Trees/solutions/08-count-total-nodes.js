/**
 * Q8 — Count Total Nodes
 * Difficulty: Easy
 * Expected:   O(n) time · O(h) space
 * Problem:    ../questions/08-count-total-nodes.md
 */

function countNodes(root) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// Tree: [1, 2, 3, 4, 5, 6] → 6

module.exports = { countNodes };
