/**
 * Q9 — Count Leaf Nodes
 * Difficulty: Easy
 * Expected:   O(n) time · O(h) space
 * Problem:    ../questions/09-count-leaf-nodes.md
 */

function countLeaves(root) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
// Tree: [1, 2, 3, 4, 5] → 3  (leaves: 4, 5, 3)

module.exports = { countLeaves };
