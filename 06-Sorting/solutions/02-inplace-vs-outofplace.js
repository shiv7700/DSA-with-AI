/**
 * Q2 — In-place vs. Out-of-place
 * Difficulty: Easy (Concept Check)
 * Expected:   Understanding, no code required
 * Problem:    ../questions/02-inplace-vs-outofplace.md
 */

function inPlaceSort(arr) {
  // TODO: implement an in-place sort (O(1) extra space) and explain
}

function outOfPlaceSort(arr) {
  // TODO: implement an out-of-place sort (O(n) extra space) and explain
}

// ── quick tests ──────────────────────────────────────────────
console.log(inPlaceSort([3, 1, 2]));    // [1, 2, 3]
console.log(outOfPlaceSort([3, 1, 2])); // [1, 2, 3]

module.exports = { inPlaceSort, outOfPlaceSort };
