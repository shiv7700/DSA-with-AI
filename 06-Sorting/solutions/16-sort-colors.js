/**
 * Q16 — Sort 0s, 1s, and 2s (Dutch National Flag)
 * Difficulty: Easy
 * Expected:   O(n) time · O(1) space
 * Problem:    ../questions/16-sort-colors.md
 */

function sortColors(arr) {
  // TODO: your solution here (in place, single pass)
}

// ── quick tests ──────────────────────────────────────────────
const a = [2, 0, 2, 1, 1, 0];
sortColors(a);
console.log(a); // [0, 0, 1, 1, 2, 2]

module.exports = { sortColors };
