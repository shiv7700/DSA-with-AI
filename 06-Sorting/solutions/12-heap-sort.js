/**
 * Q12 — Heap Sort
 * Difficulty: Medium
 * Expected:   O(n log n) time all cases · O(1) space
 * Problem:    ../questions/12-heap-sort.md
 */

function heapSort(arr) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(heapSort([4, 10, 3, 5, 1])); // [1, 3, 4, 5, 10]

module.exports = { heapSort };
