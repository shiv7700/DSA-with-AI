/**
 * Q4 — O(n log n) Lower Bound
 * Difficulty: Easy (Concept Check)
 * Expected:   Understanding, no code required
 * Problem:    ../questions/04-lower-bound.md
 */

// TODO: In comments, explain:
// 1. Why no comparison sort can beat O(n log n) worst-case (decision tree argument).
// 2. How many leaves a decision tree for n elements must have (n! permutations).
// 3. Why the height of such a tree is at least log2(n!) = O(n log n).

function placeholder() {
  // This question is theory only — see the question file for the full prompt.
}

module.exports = { placeholder };
