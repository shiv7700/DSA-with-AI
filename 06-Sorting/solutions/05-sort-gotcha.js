/**
 * Q5 — JS .sort() Lexicographic Gotcha
 * Difficulty: Easy (Concept Check)
 * Expected:   Understanding + code fix
 * Problem:    ../questions/05-sort-gotcha.md
 */

function sortAscending(arr) {
  // TODO: return a correctly sorted ascending numeric array (no mutation)
}

function sortDescending(arr) {
  // TODO: return a correctly sorted descending numeric array (no mutation)
}

// ── quick tests ──────────────────────────────────────────────
console.log(sortAscending([10, 2, 1, 25, 5]));  // [1, 2, 5, 10, 25]
console.log(sortDescending([10, 2, 1, 25, 5])); // [25, 10, 5, 2, 1]

module.exports = { sortAscending, sortDescending };
