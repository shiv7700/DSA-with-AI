/**
 * Q3 — Comparison vs. Non-comparison Sorts
 * Difficulty: Easy (Concept Check)
 * Expected:   Understanding, no code required
 * Problem:    ../questions/03-comparison-vs-noncomparison.md
 */

function comparisonSort(arr) {
  // TODO: implement a comparison-based sort (uses < / >)
}

function nonComparisonSort(arr) {
  // TODO: implement a non-comparison sort (uses numeric values directly)
}

// ── quick tests ──────────────────────────────────────────────
console.log(comparisonSort([3, 1, 2]));    // [1, 2, 3]
console.log(nonComparisonSort([3, 1, 2])); // [1, 2, 3]

module.exports = { comparisonSort, nonComparisonSort };
