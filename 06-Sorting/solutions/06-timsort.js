/**
 * Q6 — V8's TimSort
 * Difficulty: Easy (Concept Check)
 * Expected:   Understanding, no code required
 * Problem:    ../questions/06-timsort.md
 */

// TODO: In comments, explain:
// 1. What TimSort is (hybrid of Merge Sort + Insertion Sort).
// 2. What a "run" is and why natural runs help performance.
// 3. Why Insertion Sort is used for short runs.
// 4. TimSort complexity: O(n log n) worst, O(n) best, O(n) space.

function placeholder() {
  // This question is theory only — see the question file for the full prompt.
}

module.exports = { placeholder };
