/**
 * Q13 — Counting Sort
 * Difficulty: Easy
 * Expected:   O(n + k) time · O(n + k) space
 * Problem:    ../questions/13-counting-sort.md
 */

function countingSort(arr) {
  // TODO: your solution here
}

// ── quick tests ──────────────────────────────────────────────
console.log(countingSort([4, 2, 2, 8, 3, 3, 1])); // [1, 2, 2, 3, 3, 4, 8]

module.exports = { countingSort };
